import { core, deque, multilevelPriority, mutex, priority, semaphore, stack } from './core';

export const lock = {
  core,
  deque,
  multilevelPriority,
  mutex,
  priority,
  semaphore,
  stack,
};

export * from './core';
